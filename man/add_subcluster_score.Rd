% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_scores.R
\name{add_subcluster_score}
\alias{add_subcluster_score}
\title{Add subcluster score columns to a CTF dynamic tibble}
\usage{
add_subcluster_score(tbl, id_cols = c("country_code", "country_name", "year"))
}
\arguments{
\item{tbl}{A tibble — one element of \code{ctfdata_list[[cluster]][[subcluster]]}.}

\item{id_cols}{Character vector of column names to treat as identifiers
(excluded from scoring). Defaults to \code{c("country_code", "country_name", "year")}.}
}
\value{
The input tibble with three additional columns: \code{score},
\code{var_count}, and \code{nonna_count}.
}
\description{
Given a single subcluster tibble from \code{ctfdata_list}, computes a row-mean
score across all numeric indicator columns (i.e. everything that is not an
identifier column) and appends three new columns:
}
\details{
\describe{
\item{\code{score}}{Row mean of all numeric indicator columns, with
\code{na.rm = TRUE} so partially-observed rows still receive a score.}
\item{\code{var_count}}{Total number of indicator columns in this subcluster
(constant for every row).}
\item{\code{nonna_count}}{Number of non-\code{NA} indicator values used to compute
\code{score} for each row.}
}

Identifier columns (\code{country_code}, \code{country_name}, \code{year}) are never
treated as indicators, regardless of their class.
}
\examples{
\dontrun{
devtools::load_all()
scored <- add_subcluster_score(ctfdata_list$institutional_environment$degree_of_integrity)
}

}
