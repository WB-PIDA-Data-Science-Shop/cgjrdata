% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_groups.R
\name{aggregate_data_list}
\alias{aggregate_data_list}
\title{Aggregate a nested data list to region or income-group averages}
\usage{
aggregate_data_list(data_list, group_col, wbcountries)
}
\arguments{
\item{data_list}{A named nested list (\code{list[[cluster]][[subcluster]]}) where
each leaf is a tibble with at least \code{country_code} and \code{year} columns.
Both \code{ctfdata_list} and \code{rawdata_list} are acceptable inputs.}

\item{group_col}{One of \code{"region"} or \code{"income_group"}. Controls both the
grouping variable and which identifier columns appear in the output:
\describe{
\item{\code{"region"}}{Groups by \code{region} + \code{region_code}; output contains
both as identifier columns.}
\item{\code{"income_group"}}{Groups by \code{income_group} alone.}
}}

\item{wbcountries}{The \code{wbcountries} classification tibble exported by this
package. Must contain \code{country_code}, \code{region}, \code{region_code},
\code{income_group}.}
}
\value{
A named nested list with the same cluster/subcluster structure as
\code{data_list}. Each leaf tibble has group-identifier columns, \code{year},
group-mean indicator columns, and a recomputed \code{score} column.
}
\description{
Takes a nested list structured as \code{list[[cluster]][[subcluster]]} (either
\code{ctfdata_list} or \code{rawdata_list}) and produces a parallel list where each
country-level leaf tibble has been replaced by a group-average tibble.
}
\details{
\strong{Aggregation logic (per subcluster):}
\enumerate{
\item World Bank classifications (\code{region}, \code{region_code}, \code{income_group}) are
joined onto each leaf tibble via \code{country_code}.
\item Rows for WB aggregate codes (e.g. \code{"WLD"}, \code{"SSA"}) that do not match
any row in \code{wbcountries} are \strong{dropped}.
\item The leaf is grouped by \code{c(group_cols, "year")} and all numeric indicator
columns are averaged (\code{na.rm = TRUE}).
\item A \code{score} column is recomputed as the row mean of the resulting
averaged indicator columns (\code{na.rm = TRUE}). If the input was already a
scored \code{ctfdata_list}, the old \code{score}, \code{var_count}, and \code{nonna_count}
columns are dropped before averaging so they do not contaminate the
indicator means.
}
}
\examples{
\dontrun{
devtools::load_all()
regionctf_list    <- aggregate_data_list(ctfdata_list, "region",       wbcountries)
incomectf_list    <- aggregate_data_list(ctfdata_list, "income_group", wbcountries)
regionrawdata_list  <- aggregate_data_list(rawdata_list, "region",       wbcountries)
incomerawdata_list  <- aggregate_data_list(rawdata_list, "income_group", wbcountries)
}

}
