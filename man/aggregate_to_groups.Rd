% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate.R
\name{aggregate_to_groups}
\alias{aggregate_to_groups}
\title{Aggregate country rows to region / income-group rows}
\usage{
aggregate_to_groups(
  tbl,
  wbcountries,
  value_col,
  agg = stats::median,
  min_n = 1
)
}
\arguments{
\item{tbl}{A long tibble with \code{unit_level}, \code{unit_code}, \code{unit_name}, and
\code{value_col} (plus whatever taxonomy/node/year/ctf_type columns identify
a row).}

\item{wbcountries}{The \code{wbcountries} classification tibble.}

\item{value_col}{Name of the column to aggregate (e.g. \code{"ctf"} or
\code{"score"}).}

\item{agg}{Aggregation function, applied as \code{agg(x, na.rm = TRUE)}.
Defaults to \link[stats:median]{stats::median} (CLIAR fidelity); pass \code{mean} to switch.}

\item{min_n}{Minimum number of non-\code{NA} countries (\code{n_inputs_obs})
required for a group's aggregated value to be kept; below that the
value is set to \code{NA}. Defaults to \code{1}.}
}
\value{
A tibble with the same identity columns as \code{tbl} (minus
\code{unit_code} / \code{unit_name}, which are replaced by the group's own),
\code{unit_level} \code{"region"} or \code{"income_group"}, \code{value_col}, \code{n_inputs},
\code{n_inputs_obs}. Region rows use \code{region_code} as \code{unit_code} and
\code{region} as \code{unit_name}; income-group rows use a slug of \code{income_group}
(e.g. \code{"high_income"}) as \code{unit_code} and \code{income_group} as \code{unit_name}.
}
\description{
Takes the \strong{country rows} of a long tibble shaped like \code{cgjr_ctf} or
\code{cgjr_scores} (\code{unit_level == "country"}; other \code{unit_level}s are
ignored) and produces the matching \code{"region"} and \code{"income_group"} rows:
for every combination of the identity columns present (everything except
\code{unit_level} / \code{unit_code} / \code{unit_name} / \code{value_col} / any existing
\code{n_inputs} / \code{n_inputs_obs}), \code{agg()} is applied across the group's
countries.
}
\details{
World Bank aggregate codes that do not match a row of \code{wbcountries} (e.g.
\code{"WLD"}, \code{"SSA"}) are dropped before aggregating, as are the handful of
territories \code{wbcountries} has no \code{income_group} for (they still
contribute to their region).

Every output row carries \code{n_inputs} (how many countries in the group
appear in \code{tbl}) and \code{n_inputs_obs} (how many of those had a non-\code{NA}
\code{value_col}) - added even when \code{tbl} had no such columns to begin with
(\code{cgjr_ctf}'s country rows carry neither; its region/income rows still
get both, so a consumer can always see how many countries backed a group
value). If \code{n_inputs_obs < min_n}, the aggregated value is set to \code{NA}
(the row is still emitted, with its counts intact) - the default
\code{min_n = 1} never trims a row on its own, since \code{agg(..., na.rm = TRUE)}
already yields \code{NA} once there is nothing left to aggregate.
}
\examples{
\dontrun{
devtools::load_all()
ctf_country <- build_ctf_tbl(cgjr_crosswalk)
aggregate_to_groups(ctf_country, wbcountries, "ctf")
}

}
\seealso{
\code{\link[=join_wb_classifications]{join_wb_classifications()}}
}
