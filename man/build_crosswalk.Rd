% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{build_crosswalk}
\alias{build_crosswalk}
\title{Assemble the annotated CGJR crosswalk}
\usage{
build_crosswalk(
  crosswalk,
  taxonomy = cgjr_taxonomy,
  catalogue = cliaretl::db_variables_final,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic,
  ctf_static = cliaretl::closeness_to_frontier_static
)
}
\arguments{
\item{crosswalk}{The crosswalk tibble (CSV columns \code{cluster}, \code{subcluster},
\code{sub_subcluster}, \code{indicator_num}, \code{indicator}, \code{source}, \code{variable},
\code{note}).}

\item{taxonomy}{The leaf-node taxonomy. Defaults to \code{cgjr_taxonomy}.}

\item{catalogue}{The \code{cliaretl} variable catalogue. Defaults to
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}

\item{ctf_dynamic, ctf_static}{The dynamic and static Closeness-to-Frontier
panels, passed to \code{\link[=classify_crosswalk]{classify_crosswalk()}}. Default to the \code{cliaretl}
objects.}
}
\value{
A tibble - the annotated \code{cgjr_crosswalk}.
}
\description{
Takes the hand-edited crosswalk table (read from
\code{data-raw/input/cgjr_crosswalk.csv} and passed through
\code{\link[=check_crosswalk_schema]{check_crosswalk_schema()}}) and returns the annotated \code{cgjr_crosswalk}
package object: one row per \verb{(indicator x leaf)}, every CSV row kept
(including unresolved ones), with taxonomy metadata, \code{cliaretl} catalogue
metadata, and the \code{\link[=classify_crosswalk]{classify_crosswalk()}} eligibility flags joined on.
}
\details{
Rows whose \code{variable} is \code{NA} or not a \code{cliaretl} code keep \code{NA} in every
catalogue-metadata column; their flag columns are still populated
(\code{cliaretl_status} will be \code{"unresolved"} / \code{"not_in_cliaretl"}).
}
\examples{
\dontrun{
devtools::load_all()
cgjr_crosswalk <- build_crosswalk(crosswalk_csv, cgjr_taxonomy)
}

}
\seealso{
\code{\link[=check_crosswalk_schema]{check_crosswalk_schema()}}, \code{\link[=classify_crosswalk]{classify_crosswalk()}},
\code{\link[=validate_crosswalk]{validate_crosswalk()}}, \code{\link[=resolve_leaf]{resolve_leaf()}}
}
