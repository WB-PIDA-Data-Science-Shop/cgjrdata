% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctf.R
\name{build_ctf_tbl}
\alias{build_ctf_tbl}
\title{Build the long, indicator-grain CTF table (country level)}
\usage{
build_ctf_tbl(
  crosswalk = cgjr_crosswalk,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic,
  ctf_static = cliaretl::closeness_to_frontier_static,
  id_cols = c("country_code", "country_name", "year")
)
}
\arguments{
\item{crosswalk}{The annotated crosswalk (\code{cgjr_crosswalk}) - needs
\code{variable}, \code{cluster}, \code{subcluster}, \code{sub_subcluster}, \code{leaf},
\code{indicator}, \code{dynamic_eligible}, \code{static_eligible}.}

\item{ctf_dynamic, ctf_static}{The dynamic and static CTF panels. Default to
the \code{cliaretl} objects.}

\item{id_cols}{Identifier columns to carry from each panel. Defaults to
\code{c("country_code", "country_name", "year")} (the static panel has no
\code{year}, so it is dropped there automatically).}
}
\value{
A tibble with columns \code{unit_level} (always \code{"country"}),
\code{unit_code}, \code{unit_name}, \code{year}, \code{ctf_type}, \code{cluster}, \code{subcluster},
\code{sub_subcluster}, \code{leaf}, \code{indicator}, \code{variable}, \code{ctf}.
}
\description{
For each \code{ctf_type} (\code{"dynamic"}, \code{"static"}), take every annotated
crosswalk row flagged eligible for that type (\code{dynamic_eligible} /
\code{static_eligible}), slice its \code{variable} column out of the corresponding
\code{cliaretl} CTF panel, and reshape to long form. The two types are stacked
into one tibble.
}
\details{
A \code{variable} reused across two leaves (allowed by
\code{\link[=check_crosswalk_schema]{check_crosswalk_schema()}}) fans out to one row per \verb{(leaf, indicator)}.
Crosswalk rows that are unresolved, not in \code{cliaretl}, or not eligible for
a given type simply contribute no rows for that type.

Country rows only. Region / income-group rows are added afterwards by
\code{aggregate_to_groups()}.
}
\examples{
\dontrun{
devtools::load_all()
build_ctf_tbl(cgjr_crosswalk)
}

}
\seealso{
\code{\link[=build_raw_tbl]{build_raw_tbl()}}. The rollup to node scores is \code{roll_up_scores()}
(R/scores.R).
}
