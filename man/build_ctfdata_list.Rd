% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{build_ctfdata_list}
\alias{build_ctfdata_list}
\title{Build the nested CTF dynamic data list from the crosswalk}
\usage{
build_ctfdata_list(
  crosswalk = cgjr_crosswalk,
  taxonomy = cgjr_taxonomy,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic,
  catalogue = cliaretl::db_variables_final,
  id_cols = c("country_code", "country_name", "year"),
  validate = TRUE
)
}
\arguments{
\item{crosswalk}{Indicator -> leaf crosswalk (default \code{cgjr_crosswalk}).}

\item{taxonomy}{Leaf-node taxonomy (default \code{cgjr_taxonomy}).}

\item{ctf_dynamic}{The dynamic CTF panel. Defaults to
\link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic}.}

\item{catalogue}{Variable catalogue used for the eligibility classification.
Defaults to \link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}

\item{id_cols}{Identifier columns to carry onto every leaf. Defaults to
\code{c("country_code", "country_name", "year")}.}

\item{validate}{If \code{TRUE} (default), emit the \code{\link[=validate_crosswalk]{validate_crosswalk()}}
eligibility warning during the build.}
}
\value{
A named nested list following the taxonomy hierarchy; each leaf is
a tibble of \code{id_cols} plus one CTF-scaled column per contributing
indicator.
}
\description{
Pulls \link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic} once and slices it into the
nested taxonomy structure defined by \code{cgjr_taxonomy} / \code{cgjr_crosswalk}.
The result mirrors the taxonomy's hierarchy: two levels for most
subclusters (\code{list[[cluster]][[subcluster]]}) and three for Public
Financial Management (\code{list[[cluster]][[subcluster]][[sub_subcluster]]}).
}
\details{
Only rows that pass \code{\link[=validate_crosswalk]{validate_crosswalk()}}'s dynamic-panel checks
contribute columns: rows with \code{check == "not_dynamic_eligible"},
\code{"not_in_catalogue"}, or \code{"unresolved"} are excluded (their variables are
not columns of the panel). Rows flagged \code{"not_family_aggregate_eligible"}
\emph{are} included as columns — they are valid panel columns — but
\code{validate_crosswalk()} warns about them so the caller can decide whether to
trust the resulting subcluster score.

Leaf nodes with no contributing columns (e.g. PFM's empty sub-subclusters,
or a subcluster whose every indicator is dynamic-ineligible) are
represented as a zero-row tibble carrying only the identifier columns — a
valid, empty structure rather than an error.
}
\examples{
\dontrun{
devtools::load_all()
ctfdata_list <- build_ctfdata_list()
}

}
\seealso{
\code{\link[=build_rawdata_list]{build_rawdata_list()}}, \code{\link[=validate_crosswalk]{validate_crosswalk()}}, \code{\link[=score_ctfdata_list]{score_ctfdata_list()}}
}
