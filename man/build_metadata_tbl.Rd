% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{build_metadata_tbl}
\alias{build_metadata_tbl}
\title{Build the combined variable-metadata tibble from the crosswalk}
\usage{
build_metadata_tbl(
  crosswalk = cgjr_crosswalk,
  taxonomy = cgjr_taxonomy,
  catalogue = cliaretl::db_variables_final
)
}
\arguments{
\item{crosswalk}{Indicator -> leaf crosswalk (default \code{cgjr_crosswalk}).}

\item{taxonomy}{Leaf-node taxonomy (default \code{cgjr_taxonomy}).}

\item{catalogue}{Variable catalogue. Defaults to
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}
}
\value{
A tibble with one row per crosswalk row.
}
\description{
Joins the CGJR crosswalk to \link[cliaretl:db_variables_final]{cliaretl::db_variables_final} and to
\code{cgjr_taxonomy}, producing one row per taxonomy indicator with its display
name, stated source, resolved \code{cliaretl} variable, catalogue metadata,
cluster/subcluster/sub-subcluster keys and numbers, and two derived
eligibility flags.
}
\details{
Every crosswalk row is kept — including unresolved indicators
(\code{variable == NA}) and dynamic-ineligible ones — so the table is a
complete record of the taxonomy. Consumers filter on
\code{dynamic_indicator_eligible} / \code{family_aggregate_eligible} as needed.
}
\seealso{
\code{\link[=validate_crosswalk]{validate_crosswalk()}}
}
