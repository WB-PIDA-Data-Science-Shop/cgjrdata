% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw.R
\name{build_raw_tbl}
\alias{build_raw_tbl}
\title{Build the long, indicator-grain raw source table (country level)}
\usage{
build_raw_tbl(
  crosswalk = cgjr_crosswalk,
  id_cols = c("country_code", "country_name", "year")
)
}
\arguments{
\item{crosswalk}{The annotated crosswalk (\code{cgjr_crosswalk}) - needs
\code{variable}, \code{cluster}, \code{subcluster}, \code{sub_subcluster}, \code{leaf},
\code{indicator}.}

\item{id_cols}{Identifier columns passed to \code{\link[=extract_cliar_data]{extract_cliar_data()}}.
Defaults to \code{c("country_code", "country_name", "year")}.}
}
\value{
A tibble with columns \code{unit_level} (always \code{"country"}),
\code{unit_code}, \code{unit_name}, \code{year}, \code{cluster}, \code{subcluster},
\code{sub_subcluster}, \code{leaf}, \code{indicator}, \code{variable}, \code{value}.
}
\description{
Pulls every resolved crosswalk \code{variable} from its original source dataset
via \code{\link[=extract_cliar_data]{extract_cliar_data()}} with \code{type = "raw"} and reshapes to long form.
Variables with no raw source (family aggregates, static-only codes with no
dedicated raw dataset) are dropped. A \code{variable} reused across two leaves
fans out to one row per \verb{(leaf, indicator)}.
}
\details{
Country rows only - \code{cgjr_raw} has no group-aggregate rows.
}
\examples{
\dontrun{
devtools::load_all()
build_raw_tbl(cgjr_crosswalk)
}

}
\seealso{
\code{\link[=build_ctf_tbl]{build_ctf_tbl()}}, \code{\link[=extract_cliar_data]{extract_cliar_data()}}
}
