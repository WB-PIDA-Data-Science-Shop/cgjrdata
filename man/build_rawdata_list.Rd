% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{build_rawdata_list}
\alias{build_rawdata_list}
\title{Build the nested raw source data list from the crosswalk}
\usage{
build_rawdata_list(
  crosswalk = cgjr_crosswalk,
  taxonomy = cgjr_taxonomy,
  id_cols = c("country_code", "country_name", "year"),
  validate = FALSE
)
}
\arguments{
\item{crosswalk}{Indicator -> leaf crosswalk (default \code{cgjr_crosswalk}).}

\item{taxonomy}{Leaf-node taxonomy (default \code{cgjr_taxonomy}).}

\item{id_cols}{Identifier columns. Defaults to \code{c("country_code", "country_name", "year")}.}

\item{validate}{If \code{TRUE} (default), emit the \code{\link[=validate_crosswalk]{validate_crosswalk()}}
eligibility warning during the build.}
}
\value{
A named nested list following the taxonomy hierarchy; each leaf is
a tibble of identifier columns plus one raw column per resolvable
indicator.
}
\description{
The raw-data counterpart of \code{\link[=build_ctfdata_list]{build_ctfdata_list()}}. For each taxonomy leaf,
the assigned variables are fetched from their original source datasets via
\code{\link[=extract_cliar_data]{extract_cliar_data()}} with \code{type = "raw"} and assembled into the same
nested hierarchy.
}
\details{
Unlike the dynamic panel, raw coverage is independent of the
\verb{benchmark_dynamic_*} flags: a leaf that is empty in \code{ctfdata_list} (PFM,
SOE governance) can still carry raw PEFA / OECD-PMR values here. Leaves
whose variables resolve to no raw source are returned as a zero-row tibble
with identifier columns only.
}
\examples{
\dontrun{
devtools::load_all()
rawdata_list <- build_rawdata_list()
}

}
\seealso{
\code{\link[=build_ctfdata_list]{build_ctfdata_list()}}, \code{\link[=extract_cliar_data]{extract_cliar_data()}}
}
