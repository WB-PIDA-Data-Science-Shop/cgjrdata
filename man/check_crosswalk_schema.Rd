% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{check_crosswalk_schema}
\alias{check_crosswalk_schema}
\title{Check the structural integrity of the CGJR crosswalk and taxonomy}
\usage{
check_crosswalk_schema(crosswalk = cgjr_crosswalk, taxonomy = cgjr_taxonomy)
}
\arguments{
\item{crosswalk}{The indicator crosswalk. Defaults to \code{cgjr_crosswalk}.}

\item{taxonomy}{The leaf-node taxonomy. Defaults to \code{cgjr_taxonomy}.}
}
\value{
Invisibly, \code{crosswalk}. Errors on the full batch of violations
found.
}
\description{
A \code{cliaretl}-free sanity check on the two hand-edited tables
(\code{cgjr_crosswalk} and \code{cgjr_taxonomy}, read from \verb{data-raw/input/*.csv} at
build time). Run it in \verb{data-raw/source/00b-cgjr-taxonomy-crosswalk.R}
immediately after reading the CSVs and before \code{\link[=validate_crosswalk]{validate_crosswalk()}}.
}
\details{
All violations are collected and reported together in a single \code{stop()}
(the check never fails on the first problem). The checks are:

\enumerate{
\item Both tables carry their required columns.
\item No \code{NA} in the crosswalk's structural columns (\code{cluster},
\code{subcluster}, \code{indicator_num}, \code{indicator}). \code{sub_subcluster},
\code{variable} and \code{note} are legitimately blank.
\item Every \verb{(cluster, subcluster, sub_subcluster)} combination in the
crosswalk exists as a leaf row in the taxonomy.
\item Taxonomy leaf keys \verb{(cluster, subcluster, sub_subcluster)} are
unique - one row per leaf.
\item The derived \code{leaf} key, \code{dplyr::coalesce(sub_subcluster, subcluster)}, is unique across the taxonomy. The tidy build uses bare
\code{leaf} as a join key, so no \code{subcluster} key may collide with a
\code{sub_subcluster} key.
\item \code{indicator_num} is unique within each leaf.
\item \code{indicator} is unique within each leaf. Together with (6) this
guarantees a 1:1 \code{indicator_num} <-> \code{indicator} mapping within every
leaf.
\item Each non-\code{NA} \code{variable} appears at most once within a leaf.
}
}
\examples{
\dontrun{
devtools::load_all()
check_crosswalk_schema(cgjr_crosswalk, cgjr_taxonomy)
}

}
\seealso{
\code{\link[=validate_crosswalk]{validate_crosswalk()}} and \code{\link[=classify_crosswalk]{classify_crosswalk()}} for the
\code{cliaretl}-dependent eligibility classification.
}
