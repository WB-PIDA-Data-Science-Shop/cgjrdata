% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eligibility.R
\name{classify_crosswalk}
\alias{classify_crosswalk}
\title{Classify every CGJR crosswalk row against \code{cliaretl}}
\usage{
classify_crosswalk(
  crosswalk,
  catalogue = cliaretl::db_variables_final,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic,
  ctf_static = cliaretl::closeness_to_frontier_static
)
}
\arguments{
\item{crosswalk}{A crosswalk tibble with at least \code{cluster}, \code{subcluster},
\code{sub_subcluster}, \code{indicator_num}, \code{indicator} and \code{variable} columns
(e.g. \code{cgjr_crosswalk}).}

\item{catalogue}{The variable catalogue. Defaults to
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}

\item{ctf_dynamic}{The dynamic CTF panel whose columns define dynamic-panel
membership. Defaults to \link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic}.}

\item{ctf_static}{The static CTF cross-section whose columns define
static-panel membership. Defaults to
\link[cliaretl:closeness_to_frontier_static]{cliaretl::closeness_to_frontier_static}.}
}
\value{
A tibble with one row per crosswalk row (same order) and columns:
\describe{
\item{\code{cluster}, \code{subcluster}, \code{sub_subcluster}, \code{indicator_num},
\code{indicator}, \code{variable}}{copied from \code{crosswalk}.}
\item{\code{in_cliaretl}}{\code{variable} appears in \code{catalogue$variable} (the
\code{cliaretl} variable catalogue).}
\item{\code{in_dynamic_panel}}{\code{variable} is a column of \code{ctf_dynamic}.}
\item{\code{in_static_panel}}{\code{variable} is a column of \code{ctf_static}.}
\item{\code{dynamic_eligible}}{\code{in_dynamic_panel} and the catalogue flags the
variable \code{benchmark_dynamic_indicator == "Yes"}.}
\item{\code{static_eligible}}{\code{in_static_panel} (there is no separate static
indicator flag in the catalogue).}
\item{\code{cliaretl_status}}{\code{"unresolved"} (\code{variable} is \code{NA}),
\code{"not_in_cliaretl"} (set but absent from the \code{cliaretl} catalogue),
or \code{"resolved"}.}
}
}
\description{
For each row of a CGJR crosswalk, resolve its \code{variable} code against the
\code{cliaretl} catalogue and the two Closeness-to-Frontier panels and derive a
set of membership / eligibility flags. The result has one row per input
row, in the same order, so it can be column-bound onto the crosswalk by
\code{\link[=build_crosswalk]{build_crosswalk()}}.
}
\examples{
\dontrun{
devtools::load_all()
classify_crosswalk(cgjr_crosswalk)
}

}
\seealso{
\code{\link[=validate_crosswalk]{validate_crosswalk()}}, \code{\link[=check_crosswalk_schema]{check_crosswalk_schema()}}
}
