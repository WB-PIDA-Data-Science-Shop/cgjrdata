% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_cliar_data.R
\name{extract_cliar_data}
\alias{extract_cliar_data}
\title{Extract CLIAR indicator data}
\usage{
extract_cliar_data(
  variables = NULL,
  type = c("dynamic", "static", "raw"),
  id_vars = NULL
)
}
\arguments{
\item{variables}{Character vector of variable names to extract, matching
the \code{variable} column of \code{cliaretl::db_variables_final}. If \code{NULL}
(default), all variables available in the chosen \code{type} are returned.}

\item{type}{One of \code{"raw"}, \code{"dynamic"}, or \code{"static"} (default \code{"dynamic"}).
\describe{
\item{\code{"raw"}}{Returns data from the original source datasets, joined
on \code{country_code} and \code{year}.}
\item{\code{"dynamic"}}{Returns columns from
\code{cliaretl::closeness_to_frontier_dynamic} (CTF-scaled panel data).}
\item{\code{"static"}}{Returns columns from
\code{cliaretl::closeness_to_frontier_static} (CTF-scaled cross-section).}
}}

\item{id_vars}{Character vector of identifier columns to always include
alongside the requested variables. Defaults to \code{c("country_code", "country_name", "year")} for \code{"raw"} and \code{"dynamic"} types, and
\code{c("country_code", "country_name")} for \code{"static"}.}
}
\value{
A tibble containing the requested identifier columns and variables.
}
\description{
An API-style accessor for variables stored in the \code{cliaretl} package. Pass
one or more variable names (as they appear in \code{cliaretl::db_variables_final$variable})
and specify whether you want the raw source data, the closeness-to-frontier
dynamic panel, or the closeness-to-frontier static cross-section.
}
\examples{
# Degree of Integrity indicators from the dynamic CTF dataset
doi_vars <- cliaretl::db_variables_final |>
  dplyr::filter(family_name == "Degree of Integrity", !is.na(variable)) |>
  dplyr::pull(variable)

extract_cliar_data(doi_vars, type = "dynamic")
extract_cliar_data(doi_vars, type = "static")
extract_cliar_data(doi_vars, type = "raw")

}
