% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{resolve_leaf}
\alias{resolve_leaf}
\title{Derive the \code{leaf} key for a crosswalk / taxonomy row}
\usage{
resolve_leaf(sub_subcluster, subcluster)
}
\arguments{
\item{sub_subcluster, subcluster}{Character vectors of equal length
(e.g. \code{crosswalk$sub_subcluster}, \code{crosswalk$subcluster}).}
}
\value{
A character vector: \code{sub_subcluster} where non-\code{NA}, else
\code{subcluster}.
}
\description{
A leaf node is identified by its \code{sub_subcluster} key where it has one
(only the Public Financial Management branch does), otherwise by its
\code{subcluster} key. \code{check_crosswalk_schema()} guarantees the result is
unique across the taxonomy, so \code{leaf} is a valid standalone join key for
the tidy build (\code{build_ctf_tbl()}, \code{build_raw_tbl()}, \code{roll_up_scores()}).
}
\examples{
resolve_leaf(c(NA, "public_procurement"), c("digital_and_data", "public_financial_management"))

}
