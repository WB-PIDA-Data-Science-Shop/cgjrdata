% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scores.R
\name{roll_up_scores}
\alias{roll_up_scores}
\title{Roll up indicator-grain CTF values to node scores}
\usage{
roll_up_scores(ctf_tbl, taxonomy = cgjr_taxonomy)
}
\arguments{
\item{ctf_tbl}{Country-level, indicator-grain CTF values (the country rows
of \code{cgjr_ctf}, e.g. from \code{\link[=build_ctf_tbl]{build_ctf_tbl()}}). Needs \code{unit_level},
\code{unit_code}, \code{unit_name}, \code{year}, \code{ctf_type}, \code{leaf}, \code{ctf}.}

\item{taxonomy}{The leaf-node taxonomy. Defaults to \code{cgjr_taxonomy}.}
}
\value{
A tibble with one row per \verb{unit x year x ctf_type x node}:
\code{unit_level}, \code{unit_code}, \code{unit_name}, \code{year}, \code{ctf_type}, \code{node_level}
(\code{"subcluster"} / \code{"sub_subcluster"} / \code{"cluster"} / \code{"overall"} - real
taxonomy tiers, \code{"sub_subcluster"} occurring only under Public Financial
Management), \code{node} (the operative key at that level, or \code{"overall"}),
\code{cluster} / \code{subcluster} / \code{sub_subcluster} (ancestry filled to the
node's depth, \code{NA} deeper), \code{score}, \code{n_inputs}, \code{n_inputs_obs}.
}
\description{
Rolls the country-level, indicator-grain \code{cgjr_ctf} up to node scores at
the taxonomy's real tiers by repeated equal-weight averaging: a leaf's
score = mean of its indicators' \code{ctf} values; a branching subcluster's
score (Public Financial Management only) = mean of its \code{sub_subcluster}
scores; a cluster's score = mean of its subclusters' scores (using each
plain subcluster's own score, or PFM's subcluster-level aggregate);
overall = mean of the cluster scores. \code{NA} is lenient (\code{na.rm = TRUE}) at
every stage, and every node row records \code{n_inputs} (how many immediate
children) and \code{n_inputs_obs} (how many of those were non-\code{NA}).
}
\details{
The finest grain is completed against every row of \code{taxonomy} before
rolling further up, so a leaf with zero eligible indicators for a given
\code{ctf_type} (or for every \code{ctf_type}) still gets a row - \code{score = NA},
\code{n_inputs = 0L} - for every \verb{(unit, year, ctf_type)} combination present
in \code{ctf_tbl}, rather than silently not appearing.

There is no stored "is this the finest grain" flag - every plain
subcluster reports once, at \code{node_level == "subcluster"}, and \emph{is} the
finest grain for its branch; only Public Financial Management also has
\code{node_level == "sub_subcluster"} rows one level finer than its own
\code{subcluster} row. To get every finest-grain node regardless of depth:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{branching <- unique(scores$subcluster[scores$node_level == "sub_subcluster"])
finest <- scores[scores$node_level \%in\% c("subcluster", "sub_subcluster") &
                 !(scores$node_level == "subcluster" & scores$subcluster \%in\% branching), ]
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
devtools::load_all()
roll_up_scores(build_ctf_tbl(cgjr_crosswalk), cgjr_taxonomy)
}

}
\seealso{
\code{\link[=build_ctf_tbl]{build_ctf_tbl()}}
}
