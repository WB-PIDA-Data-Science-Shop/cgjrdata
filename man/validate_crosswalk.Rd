% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{validate_crosswalk}
\alias{validate_crosswalk}
\title{Validate the CGJR crosswalk against \code{cliaretl} eligibility flags}
\usage{
validate_crosswalk(
  crosswalk,
  catalogue = cliaretl::db_variables_final,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic
)
}
\arguments{
\item{crosswalk}{A crosswalk tibble with at least \code{variable}, \code{cluster},
\code{subcluster}, \code{sub_subcluster}, and \code{indicator} columns (e.g.
\code{cgjr_crosswalk}).}

\item{catalogue}{The variable catalogue. Defaults to
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}

\item{ctf_dynamic}{The dynamic CTF panel whose columns define
panel membership. Defaults to \link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic}.}
}
\value{
Invisibly, a tibble with one row per crosswalk row and the columns
\code{variable}, \code{cluster}, \code{subcluster}, \code{sub_subcluster}, \code{indicator},
\code{in_catalogue}, \code{in_dynamic_panel}, \code{benchmark_dynamic_indicator},
\code{benchmark_dynamic_family_aggregate}, and \code{check}.
}
\description{
Checks every row of a CGJR crosswalk table against
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final} and
\link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic}, classifying each into one of:
}
\details{
\describe{
\item{\code{"ok"}}{Variable is in the catalogue, is a column of the dynamic
CTF panel, has \code{benchmark_dynamic_indicator == "Yes"}, and has
\code{benchmark_dynamic_family_aggregate} in \code{c("Yes", "Partial")} — safe to
average into a subcluster score.}
\item{\code{"unresolved"}}{\code{variable} is \code{NA} — the taxonomy lists an
indicator but no \code{cliaretl} code could be confirmed for it.}
\item{\code{"not_in_catalogue"}}{\code{variable} is not \code{NA} but does not appear in
\code{db_variables_final$variable}.}
\item{\code{"not_dynamic_eligible"}}{Variable is in the catalogue but is not a
column of \code{closeness_to_frontier_dynamic}, or is flagged
\code{benchmark_dynamic_indicator != "Yes"} — it cannot enter the dynamic
panel at all.}
\item{\code{"not_family_aggregate_eligible"}}{Variable is in the dynamic panel
but \code{benchmark_dynamic_family_aggregate} is \code{"No"} — including it in a
subcluster row-mean is not sanctioned by \code{cliaretl}'s own metadata.}
}

A \code{warning} is emitted listing every non-\code{"ok"} row (variable, path, which
check failed). Rows are never silently dropped or silently included — that
is the caller's decision, informed by this report.
}
\examples{
\dontrun{
devtools::load_all()
validate_crosswalk(cgjr_crosswalk)
}

}
