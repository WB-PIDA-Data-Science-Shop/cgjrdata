% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eligibility.R
\name{validate_crosswalk}
\alias{validate_crosswalk}
\title{Validate the CGJR crosswalk against \code{cliaretl} and warn on gaps}
\usage{
validate_crosswalk(
  crosswalk,
  catalogue = cliaretl::db_variables_final,
  ctf_dynamic = cliaretl::closeness_to_frontier_dynamic,
  ctf_static = cliaretl::closeness_to_frontier_static
)
}
\arguments{
\item{crosswalk}{A crosswalk tibble with at least \code{cluster}, \code{subcluster},
\code{sub_subcluster}, \code{indicator_num}, \code{indicator} and \code{variable} columns
(e.g. \code{cgjr_crosswalk}).}

\item{catalogue}{The variable catalogue. Defaults to
\link[cliaretl:db_variables_final]{cliaretl::db_variables_final}.}

\item{ctf_dynamic}{The dynamic CTF panel whose columns define dynamic-panel
membership. Defaults to \link[cliaretl:closeness_to_frontier_dynamic]{cliaretl::closeness_to_frontier_dynamic}.}

\item{ctf_static}{The static CTF cross-section whose columns define
static-panel membership. Defaults to
\link[cliaretl:closeness_to_frontier_static]{cliaretl::closeness_to_frontier_static}.}
}
\value{
Invisibly, the \code{\link[=classify_crosswalk]{classify_crosswalk()}} tibble.
}
\description{
Runs \code{\link[=classify_crosswalk]{classify_crosswalk()}} and emits a single \code{warning()} listing every
row that will contribute \strong{no} Closeness-to-Frontier data, grouped into:
}
\details{
\describe{
\item{\code{unresolved}}{\code{variable} is \code{NA} - the taxonomy names an indicator
but no \code{cliaretl} code has been confirmed for it.}
\item{\code{not_in_cliaretl}}{\code{variable} is set but does not appear in
\code{db_variables_final$variable}.}
\item{\code{no_ctf_panel}}{\code{variable} resolves to a \code{cliaretl} catalogue entry
but is eligible for neither the dynamic nor the static CTF panel.}
}

Rows that are static-eligible only (not dynamic) are \strong{not} warned about -
they are fine, they simply produce static rows only. Their count is
included in the summary message for visibility.

Rows are never silently dropped or included by this function - that is the
build's decision, informed by this report.
}
\examples{
\dontrun{
devtools::load_all()
validate_crosswalk(cgjr_crosswalk)
}

}
\seealso{
\code{\link[=classify_crosswalk]{classify_crosswalk()}}, \code{\link[=check_crosswalk_schema]{check_crosswalk_schema()}}
}
